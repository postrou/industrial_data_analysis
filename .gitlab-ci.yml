image: alpine

pages:
  script:
  - apk --no-cache add py3-pip python-dev
  - pip install sphinx
  - apk --no-cache add make
  - cd doc
  - make html
  - mv _build/html ../public
  - cd ..
  artifacts:
    paths:
    - public
  only:
  - master
